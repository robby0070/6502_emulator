project('template', 'cpp',default_options : ['cpp_std=c++20'])

catch2 = dependency('catch2')
spdlog = dependency('spdlog')
fmt = dependency('fmt')

main = files('main.cpp')

subdir('src')
subdir('tests')
